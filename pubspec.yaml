name: flame
description: Industrial monitoring app for flow & temperature.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pie_chart: ^5.3.2
  fl_chart: ^0.66.0

flutter:
  uses-material-design: true
  assets:
    - assets/image.png

dev_dependencies:
  flutter_lints: ^3.0.1
    
